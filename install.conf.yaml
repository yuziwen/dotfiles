- defaults:
    link:
      relink: true
      create: true
      relink: true
      

- clean: ['~']

- link:
    # ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    # ~/.vim: .vim
    # ~/.vimrc: .vimrc
    ~/.config/i3/config: config/i3/config
    # ~/.gitconfig: gitconfig
    ~/.xinitrc: xinitrc
    

- shell:
  - [git submodule update --init --recursive, Installing submodules]